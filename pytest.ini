[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

markers =
    frontend: tests for frontend UI components
    backend: tests for backend logic
    integration: tests that cover multiple components

addopts = --strict-markers
